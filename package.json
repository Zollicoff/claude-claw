{
  "name": "claude-claw",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "gateway",
    "mcp-server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf gateway/dist mcp-server/dist",
    "dev": "npm run dev --workspace=gateway"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
